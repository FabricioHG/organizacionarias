/**
 * @file
 * theme_arias behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.themeArias = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
